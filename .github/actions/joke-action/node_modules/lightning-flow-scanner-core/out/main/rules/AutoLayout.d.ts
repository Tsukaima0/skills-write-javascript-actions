import * as core from '../internals/internals';
import { RuleCommon } from '../models/RuleCommon';
export declare class AutoLayout extends RuleCommon implements core.IRuleDefinition {
    constructor();
    execute(flow: core.Flow, options?: {
        expression: string;
    }): core.RuleResult;
}
