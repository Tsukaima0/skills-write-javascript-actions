import { IRuleDefinition } from '../interfaces/IRuleDefinition';
export declare class RuleParser {
    static parseRuleFile(filePath: string): IRuleDefinition | undefined;
    private static extractRuleDefinition;
}
